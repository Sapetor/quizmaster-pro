# QuizMaster Pro Docker Environment Configuration
# Copy this file to .env and adjust values for your environment

# ===========================================
# Application Configuration
# ===========================================
NODE_ENV=production

# Debug Configuration (Production Settings)
DEBUG_ENABLED=false
DEBUG_CURRENT_LEVEL=1

# ===========================================
# Network Configuration
# ===========================================
PORT=3000
HOST=0.0.0.0

# ===========================================
# GitLab Integration
# ===========================================
# GitLab server URL (adjust to your GitLab instance)
GITLAB_URL=http://gitlab.local:8080
# Alternative: GITLAB_URL=https://your-gitlab-domain.com

# GitLab Personal Access Token (optional, for API access)
GITLAB_TOKEN=

# GitLab SSH Key Path (if using SSH for git operations)
GITLAB_SSH_KEY=/app/.ssh/id_rsa

# ===========================================
# Database/Storage Configuration
# ===========================================
# Results storage path
RESULTS_PATH=/app/results

# Upload directory
UPLOADS_PATH=/app/uploads

# Maximum file size for uploads (in bytes)
MAX_FILE_SIZE=10485760

# ===========================================
# Security Configuration
# ===========================================
# CORS Origins (adjust for your network)
CORS_ORIGINS=http://localhost:3000,http://192.168.1.*,http://10.0.0.*,http://172.16.*

# Session secret (generate a strong random string)
SESSION_SECRET=your-super-secure-session-secret-change-this

# ===========================================
# Performance Configuration
# ===========================================
# Enable gzip compression
COMPRESSION_ENABLED=true

# QR Code cache settings
QR_CACHE_ENABLED=true
QR_CACHE_TTL=3600

# ===========================================
# Monitoring Configuration
# ===========================================
# Health check endpoint
HEALTH_CHECK_ENABLED=true

# Metrics collection
METRICS_ENABLED=false

# ===========================================
# Docker Network Configuration
# ===========================================
# Docker network subnet (adjust to avoid conflicts)
DOCKER_SUBNET=172.20.0.0/16
DOCKER_GATEWAY=172.20.0.1

# Container resource limits
MEMORY_LIMIT=512m
CPU_LIMIT=0.5